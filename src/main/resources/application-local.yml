spring:
  datasource:
    url: jdbc:mysql://localhost:3306/returnWork_test
    username: root
    password: tkddbs3535
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate

  data:
    redis:
      host: localhost
      port: 6379
      lettuce:
        pool:
          max-active: 8
          max-wait: -1ms
          max-idle: 8
          min-idle: 0

  flyway:
    enabled : true
    baseline-on-migrate: true
    locations : classpath:db/migration